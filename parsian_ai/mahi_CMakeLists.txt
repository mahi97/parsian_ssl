add_library(mahi
	src/behavior/mahi/mahi.cpp

)
add_library(mahi_nodelet
        src/behavior/mahi/mahi_nodelet.cpp

)

add_executable(mahi_node
        src/behavior/mahi/mahi_node.cpp

)

target_link_libraries(mahi_node
        ${catkin_LIBRARIES}
        mahi_nodelet
        )

target_link_libraries(mahi_nodelet
	${catkin_LIBRARIES}
        ${QT_LIBRARIES}
	mahi
	
	)

target_link_libraries(mahi
	${catkin_LIBRARIES}
        ${QT_LIBRARIES}
	)

add_dependencies(mahi
        ${${PROJECT_NAME}_EXPORTED_TARGETS}
        ${catkin_EXPORTED_TARGETS}
	${PROJECT_NAME}_gencfg
        )

add_dependencies(mahi_node
        ${${PROJECT_NAME}_EXPORTED_TARGETS}
        ${catkin_EXPORTED_TARGETS}
	${PROJECT_NAME}_gencfg
        )

add_dependencies(mahi_nodelet
        ${${PROJECT_NAME}_EXPORTED_TARGETS}
        ${catkin_EXPORTED_TARGETS}
	${PROJECT_NAME}_gencfg
        )
