add_library(direct
    src/behavior/direct/direct.cpp

)

add_library(direct_nodelet
    src/behavior/direct/direct_nodelet.cpp

)

add_executable(direct_node
    src/behavior/direct/direct_node.cpp

)

target_link_libraries(direct_node
    ${catkin_LIBRARIES}
    direct_nodelet
)

target_link_libraries(direct_nodelet
    ${catkin_LIBRARIES}
    ${QT_LIBRARIES}
    direct
    ${PROJECT_NAME}_util
)

target_link_libraries(direct
    ${catkin_LIBRARIES}
    ${QT_LIBRARIES}
    ${PROJECT_NAME}_util

)

add_dependencies(direct
    ${${PROJECT_NAME}_EXPORTED_TARGETS}
    ${catkin_EXPORTED_TARGETS}
    ${PROJECT_NAME}_gencfg
)

add_dependencies(direct_node
    ${${PROJECT_NAME}_EXPORTED_TARGETS}
    ${catkin_EXPORTED_TARGETS}
    ${PROJECT_NAME}_gencfg
)

add_dependencies(direct_nodelet
    ${${PROJECT_NAME}_EXPORTED_TARGETS}
    ${catkin_EXPORTED_TARGETS}
    ${PROJECT_NAME}_gencfg
)
